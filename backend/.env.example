# One or more GoEmotions-trained models (JSON list). Two = ensemble (default,
# benchmark winner, macro-F1 0.673). One = single model, lower latency.
MODEL_NAMES=["bhadresh-savani/bert-base-go-emotion","SamLowe/roberta-base-go_emotions"]
TOP_K=5
MAX_ROWS=5000
CORS_ORIGINS=["http://localhost:3000"]
